<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Mulse.Modules\Mulse.Modules.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <OutDir>..\Service\plugins\Mulse.SamplePlugin\</OutDir>
    <!-- See Mulse.Dbms.SqlServer.csproj: forces this plugin's own NuGet dependencies (if any are added
         later) to be copied locally so the plugins/ folder stays self-contained. -->
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
  </PropertyGroup>

</Project>
